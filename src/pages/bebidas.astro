---
import Layout from "@/layouts/Layout.astro";
import { DRINKS } from "@/constants/drinks";
import Card from "@/components/Card.astro";
import Container from "@/UI/Container.astro";
---

<Layout
  title="Bebidas"
  description="Bebidas en la Ciudad de México"
>
  <Container title="BEBIDAS">
    {DRINKS.map(({ title, href, img, price }) => (
      <Card
        title={title}
        href={href}
        img={img}
        price={price ?? 0}
        imgSize="w-36"
      />
    ))}
  </Container>
</Layout>
